torch>=2.0.0,<3
# transformers>=4.48 requires torch>=2.6 to safely load .bin checkpoints (CVE-2025-32434).
# On titan (driver 535, cu121, torch 2.5.x) we cap at <4.48 to stay compatible.
# On aida (driver 560, cu124, torch 2.6+) the cap has no effect.
transformers>=4.35.0,<4.48.0
scikit-learn>=1.3.0
numpy>=1.24.0
tqdm>=4.65.0
ijson>=3.2.0
gensim>=4.3.0
pyyaml>=6.0
wandb>=0.17.0
